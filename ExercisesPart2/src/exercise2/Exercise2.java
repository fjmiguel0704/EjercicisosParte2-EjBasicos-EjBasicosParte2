package exercise2;

public class Exercise2 {

	public static void main(String[] args) {
		
		//Creamos las variables x, y, x
		int x, y, z;
		
		//Declaramos que la variable x tendrá un valor igual a 5
		x = 5;
		
		//Declaramos que la variable y será igual al valor de la variable x mas dos
		y = x + 2;
		
		//Declaramos que la variable x tendrá un valor igual a 10
		x = 10;
		
		//Declaramos que la variable z será igual al valor de la variable y multiplicado por el valor de la variable x
		z = y * x;
		
		//Le mostramos al usuario el valor de la variable z
		System.out.print(z);

	}

}
